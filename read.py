#when used in a function definition ** is used tpo collect multiple keyword arguments into a single
# it's concise and Pythonic way to merge dictionaries,instead of using .update() or loops.
# The ** operator is called the dictionary unpacking operatior
#it"unpacks" (expands) the key-value pairs of a dictionary into another dictionary or function call
#FOR DICTIONRY IF WE HAVE TO CONCATENATE WE WILL USE - **  AND IN CASE OF TUPLE WE ONLY USE SINGLE *
#global scope A varialble has global scope,if its created outside the function